# Optional. If unset, the app runs in DEMO MODE: the bundled sample reviews return
# recorded responses, so the whole UI is usable with no API key (great for a quick look).
# Set it to analyze any pasted review live with Claude.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6
